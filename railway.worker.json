{
  "$schema": "https://railway.app/railway-schema.json",
  "deploy": {
    "startCommand": "bun run scripts/pre-migrate.ts; bunx prisma migrate deploy; bun run start:worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
