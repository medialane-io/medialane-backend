{
  "$schema": "https://railway.app/railway-schema.json",
  "deploy": {
    "startCommand": "bunx prisma migrate resolve --applied 20260312000002_normalize_collection_addresses 2>/dev/null; bunx prisma migrate deploy; bun run src/index.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
